// const express = require("express");
// const router = express.Router();

// router.post("/foodData", (req, res) => {
//   try {
//     res.send([global.data, global.Catdata]); // Ensure these are populated correctly
//   } catch (error) {
//     console.error(error.message);
//     res.status(500).send("Server Error!"); // Send a proper server error response
//   }
// });

// module.exports = router;
